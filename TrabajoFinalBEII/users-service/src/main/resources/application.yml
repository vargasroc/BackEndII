eureka:
  client:
    enabled: false

server:
  port: 8086

springdoc:
  show-actuator: true
  swagger-ui:
    groups-order: DESC
    disable-swagger-default-url: true
management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always

app:
  cors:
    allowed-origins: http://localhost:3000

spring:
  application:
    name: users-service
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8083/realms/DigitalMedia
        registration:
          keycloak:
            client-id: microservicios
            client-secret: xGyIx19WJdg1nyKc9Cyy8Ybg0oSGL2br
            scope: openid
            redirect_uri: http://localhost:8085
  data:
    mongodb:
      host: localhost
      port: 27017
      password: admin
      username: root
      authentication-database: admin
      database: moviesdb
  main:
    allow-circular-references: true

dm:
  keycloak:
    realm: DigitalMedia
    serverUrl: http://localhost:8083/
    clientId: microservicios
    clientSecret: xGyIx19WJdg1nyKc9Cyy8Ybg0oSGL2br
